% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kdtree.R
\name{kdtree_build}
\alias{kdtree_build}
\title{Build a k-d tree from a numeric matrix}
\usage{
kdtree_build(data)
}
\arguments{
\item{data}{numeric matrix (\code{n} rows, \code{p} columns).}
}
\value{
an external pointer of class \code{AirRoute_kdtree}; freed
  automatically when garbage-collected.
}
\description{
Builds a k-d tree over \code{n} points in \code{p} dimensions. Rows of
\code{data} are samples; columns are features. Distances are Euclidean
in that feature space.
}
\examples{
data <- matrix(c(0, 0, 1, 0, 100, 0), ncol = 2, byrow = TRUE)
tree <- kdtree_build(data)
kdtree_query(tree, c(0, 0), k = 2)
}
